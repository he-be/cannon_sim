UIの修正必要な点
左ペイン
・Artillery　のAzとElを制御するスライダーは、0.1度単位の微調整が困難。＋とーボタンを付け、押している間0.1度/secの速度で連続的にAzとElの数値が変化し、数値表示だけでなくスライダーも連動して更新されるようにする。
・Cancel trackingボタンを削除する
・レーダーのEl表示を追加する
中央ペイン
・砲弾の爆発エフェクトが、画面中央で出るのはおかしい。砲弾が爆発した地点がレーダー範囲内のときのみ、爆発した地点で出るようにする。
・UI-13の要求が達成されていない
右ペイン
・UI-16の要求が達成されていない

追加修正点
現状は、簡単な重力計算から適切なPhysicsEngine + RK4積分に変更、実際の砲弾飛行と同じ物理計算: 重力 + 空気抵抗 + PhysicsEngineの精密な数値積分まではできているが、
レーダー座標系で正確な軌跡表示: 3D軌跡を適切にレーダー表示座標に投影　が達成できていない。以下のやり方で正確に達成する必要がある。
まず砲の座標系で軌跡を予測し、一定時間ごとにサンプリングして予測起動の3D座標点を記録する。その座標点を、レーダー基準（レーダーは追尾により自動的にAzが変化する）の座標に変換し、中央ペインの水平表示に反映する。右ペインの垂直表示にも、同様に反映する。